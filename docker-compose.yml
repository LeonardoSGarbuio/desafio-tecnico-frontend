services:
  dragon-sanctuary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dragon-sanctuary-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
