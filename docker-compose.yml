version: '3.8'

services:
  dragon-sanctuary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dragon_sanctuary_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
